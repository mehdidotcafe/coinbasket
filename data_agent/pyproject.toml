[project]
name = "data_agent"
version = "0.0.1"
description = "🧺 coinbasket data agent"
authors = [
    {name = "mehdidotcafe",email = "contact@mehdi.cafe"}
]
license = {text = "GPL3"}
readme = "README.md"
requires-python = ">=3.10,<4.0"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
"data_agent" = "data_agent:main"
"data_agent:cli" = "data_agent.cli:main"
[tool.poetry.group.dev.dependencies]
pytest = "^8.3.5"
pytest-watch = "^4.2.0"

